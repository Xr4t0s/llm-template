[project]
name = "mcp-documentation-server"
version = "0.1.0"
description = "MCP server for documentation generation"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "mcp[cli]>=1.26.0",
]

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"